import { EnterInputDirective } from './enter-input.directive';

describe('EnterInputDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterInputDirective();
    expect(directive).toBeTruthy();
  });
});
